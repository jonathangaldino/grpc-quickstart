syntax = "proto3";

message User {
  string id = 1;
  string name = 2;
  string age = 3;
}

message Empty {}

message ListUsersResponse {
  repeated User users = 1;
}

service UserService {
  rpc ListUsers (Empty) returns (ListUsersResponse);
}